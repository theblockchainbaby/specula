export default function Home() {
  return (
    <main>
      <h1>Specula playground</h1>
      <section>
        <p>
          If this HTML carries <code>data-spc</code> attributes, the SWC plugin
          is wired in correctly.
        </p>
      </section>
    </main>
  );
}

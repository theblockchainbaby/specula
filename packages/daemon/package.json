{
  "name": "@specula/daemon",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Specula daemon — owns the project, speaks the intent protocol.",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "dev": "node --import tsx src/cli.ts"
  },
  "dependencies": {
    "tailwind-merge": "^2.0.0",
    "ws": "^8.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "@types/ws": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^6.0.0"
  }
}
